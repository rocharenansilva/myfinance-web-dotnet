@model PlanoContaModel

@{
    ViewData["Title"] = "Cadastro de Plano de Contas";
}
<h1>@ViewData["Title"]</h1>

<div class="mt-3 mb-3">
  <form asp-controller="PlanoConta" asp-action="Cadastro" method="post">
    <div class="container">
      <div class="col-md-3">
        <input type="hidden" id="txtId" class="form-control" asp-for="Id" />

        <label class="form-label">Descrição</label>
        <input type="text" id="txtDescricao" class="form-control" asp-for="Descricao" />

        <div class="md-3">
          <label class="form-label mt-3 mb-0">Tipo</label>
          <div class="form-check">
            <label>@Html.RadioButtonFor(m => m.Tipo, 'D')Despesa</label>
            <label>@Html.RadioButtonFor(m => m.Tipo, 'R')Receita</label>
          </div>
        </div>

        <div class="mt-3 mb-3">
          <button type="submit" class="btn btn-success">Salvar</button>
          <button type="button" class="btn btn-secondary" onclick="voltarPlanoConta()">Cancelar</button>
        </div>

      </div>
    </div>
  </form>
</div>

<script>
  function voltarPlanoConta() {
    window.location.href = "../../PlanoConta/Index";
  }
</script>